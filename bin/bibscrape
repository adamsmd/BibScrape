#!/usr/bin/env perl6

# Load local libraries if running from a repository and not installed
if $?FILE.IO.parent.add( '../lib' ).e { use lib $?FILE.IO.parent.add( '../lib' ); }

use BibScrape::CommandLine;
use BibScrape::Main;
